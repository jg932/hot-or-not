<%- include('./partials/html-head') %>
<%- include('./partials/nav') %>

<div id="background-image">
<main>
  <h1>hello, <%= user ? user.profile.name : "spice lovers!" %></h1>
  <% if (user?.profile.avatar) { %>
    <div>
      <img 
        src="<%= user.profile.avatar %>" 
        alt="<%= user.profile.name %> avatar"
      >
    </div>
  <% } %>
    <h1>Welcome to Hot or Not!<br>a database of hot sauces</h1>
    <div id="welcome-message"> 
      <h3>You are welcome to explore our hot sauce and chili peppers listings.  Hot sauces have reviews that you can read.  
      If you log in with Google you can add reviews to existing entries and add your own spicy finds to the database yourself."</h3>
    </div>
</main>

<%- include('./partials/footer') %>
</div>